# Student Attendance System
A digital verification and tracking system built with ASP.NET Core MVC and SQL Server.

## Setup
1. Execute `db_setup.sql` in SSMS.
2. Update connection string in `appsettings.json`.
3. Run the project in Visual Studio 2022.
